# --- Frigate / MQTT ---
FRIGATE_RTSP_PASSWORD=changeme
MQTT_HOST=mosquitto
MQTT_TOPIC=frigate/events

# --- Detection tuning ---
GUN_CONF=0.6           # confidence threshold (0.0–1.0)
PERSIST_FRAMES=6       # consecutive frames required (~0.5s @12fps)
MODEL_PATH=/app/weights/gun.pt  # where the sidecar expects the YOLO weights

# --- Notifications (optional) ---
TELEGRAM_TOKEN=
TELEGRAM_CHAT_ID=
WEBHOOK_URL=           # e.g., https://your-service.example.com/alerts
